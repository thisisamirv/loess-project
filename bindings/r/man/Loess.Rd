% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Loess.R
\name{Loess}
\alias{Loess}
\title{LOESS Batch Smoothing}
\usage{
Loess(
  fraction = 0.67,
  ...,
  iterations = 3L,
  weight_function = "tricube",
  robustness_method = "bisquare",
  scaling_method = "mad",
  boundary_policy = "extend",
  confidence_intervals = NULL,
  prediction_intervals = NULL,
  return_diagnostics = FALSE,
  return_residuals = FALSE,
  return_robustness_weights = FALSE,
  zero_weight_fallback = "use_local_mean",
  auto_converge = NULL,
  cv_fractions = NULL,
  cv_method = "kfold",
  cv_k = 5L,
  parallel = TRUE,
  degree = "linear",
  dimensions = 1L,
  distance_metric = "normalized",
  surface_mode = "interpolation",
  return_se = FALSE,
  weighted_metric_weights = NULL,
  cell = NULL,
  interpolation_vertices = NULL,
  boundary_degree_fallback = NULL,
  cv_seed = NULL
)
}
\arguments{
\item{fraction}{Smoothing fraction, greater than 0 and up to 1. Default:
0.67. See Details for guidance on choosing a value.}

\item{...}{Not used; forces all subsequent arguments to be named.}

\item{iterations}{Number of robustness iterations, between 0 and 1000
(inclusive). Default: 3.}

\item{weight_function}{Kernel weight function. One of \code{"tricube"}
(default), \code{"gaussian"},
\code{"uniform"} (alias: \code{"boxcar"}),
\code{"cosine"}, \code{"epanechnikov"},
\code{"biweight"} (alias: \code{"bisquare"}), or
\code{"triangle"} (alias: \code{"triangular"}).}

\item{robustness_method}{Outlier downweighting method: \code{"bisquare"}
(default; alias: \code{"biweight"}), \code{"huber"}, or \code{"talwar"}.}

\item{scaling_method}{Residual scale estimation for robustness weights:
\code{"mad"} (default; alias: \code{"median_absolute_deviation"}),
\code{"mar"} (alias: \code{"median_absolute_residual"}), or
\code{"mean"} (alias: \code{"mean_absolute_residual"}).}

\item{boundary_policy}{Boundary handling strategy: \code{"extend"}
(default; alias: \code{"pad"}), \code{"reflect"} (alias:
\code{"mirror"}), \code{"zero"}, or
\code{"noboundary"} (alias: \code{"none"}).}

\item{confidence_intervals}{Confidence level for confidence intervals,
greater than 0 and less than 1 (e.g., 0.95). \code{NULL} (default)
disables confidence intervals.}

\item{prediction_intervals}{Confidence level for prediction intervals,
greater than 0 and less than 1 (e.g., 0.95). \code{NULL} (default)
disables prediction intervals.}

\item{return_diagnostics}{Logical; if \code{TRUE}, return fit-quality
metrics (RMSE, MAE, R-squared, AIC, etc.). Default: \code{FALSE}.}

\item{return_residuals}{Logical; if \code{TRUE}, return residuals in the
result. Default: \code{FALSE}.}

\item{return_robustness_weights}{Logical; if \code{TRUE}, return per-point
robustness weights. Default: \code{FALSE}.}

\item{zero_weight_fallback}{Fallback policy when all robustness weights drop
to zero: \code{"use_local_mean"} (default; aliases: \code{"local_mean"},
\code{"mean"}), \code{"return_original"} (alias: \code{"original"}), or
\code{"return_none"} (alias: \code{"none"}).}

\item{auto_converge}{Convergence tolerance for early stopping of robustness
iterations. \code{NULL} (default) disables early stopping.}

\item{cv_fractions}{Numeric vector of candidate fractions for
cross-validation. \code{NULL} (default) disables CV.}

\item{cv_method}{Cross-validation method: \code{"kfold"} (default) or
\code{"loocv"}.}

\item{cv_k}{Number of folds for k-fold CV, at least 2. Default: 5.}

\item{parallel}{Logical; enable parallel processing. Default: \code{TRUE}.}

\item{degree}{Local polynomial degree: \code{"constant"}, \code{"linear"}
(default), \code{"quadratic"}, \code{"cubic"}, or \code{"quartic"}.}

\item{dimensions}{Number of predictor dimensions. Default: 1.}

\item{distance_metric}{Distance metric for neighbourhood computation:
\code{"normalized"} (default), \code{"euclidean"}, \code{"manhattan"},
\code{"chebyshev"}, \code{"minkowski"}, or \code{"weighted"}.
Use \code{"minkowski:p"} to set a custom \emph{p} value.}

\item{surface_mode}{Surface evaluation mode: \code{"interpolation"}
(default) or \code{"direct"}.}

\item{return_se}{Logical; if \code{TRUE}, compute hat-matrix statistics
(effective degrees of freedom, leverage, standard errors).
Default: \code{FALSE}.}

\item{weighted_metric_weights}{Numeric vector of per-dimension weights.
Length must equal \code{dimensions}. Only used when
\code{distance_metric = "weighted"}; setting
\code{distance_metric = "weighted"} without providing this raises an
error. \code{NULL} (default) has no effect unless
\code{distance_metric = "weighted"} is set.}

\item{cell}{Cell size tuning parameter for the interpolation grid.
\code{NULL} (default) uses the library default.}

\item{interpolation_vertices}{Number of vertices in the interpolation grid.
\code{NULL} (default) uses the library default.}

\item{boundary_degree_fallback}{Logical; if \code{TRUE}, fall back to lower
polynomial degree at boundaries when fitting at the requested degree
fails. \code{NULL} (default) uses the library default.}

\item{cv_seed}{Integer seed for the cross-validation random number
generator. \code{NULL} (default) uses a random seed.}
}
\value{
A Loess object.
}
\description{
Create a stateful LOESS model for batch smoothing. This is the default
mode: it processes the entire dataset at once and supports every feature
(confidence/prediction intervals, cross-validation, GPU backend).
}
\details{
Best suited when the dataset fits in memory and you need intervals,
cross-validation, or diagnostics. For datasets that don't fit in memory or
arrive in chunks, see \code{\link{StreamingLoess}}; for point-by-point
real-time data, see \code{\link{OnlineLoess}}.

\code{fraction} is the most important parameter: it controls the size of the
local neighbourhood used at each point.\tabular{lll}{
   Range \tab Effect \tab Use case \cr
   0.1-0.3 \tab Fine detail \tab Rapidly changing signals \cr
   0.3-0.5 \tab Balanced \tab General purpose \cr
   0.5-0.7 \tab Heavy smoothing \tab Noisy data \cr
   0.7-1.0 \tab Very smooth \tab Trend extraction \cr
}
}
\examples{
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)
model <- Loess(fraction = 0.2)
result <- fit(model, x, y)
plot(x, y)
lines(x, result$y, col = "red")
}
