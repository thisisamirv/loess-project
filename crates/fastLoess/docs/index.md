Add the crate to your `Cargo.toml`:

=== "loess-rs (no_std compatible)"

    ```toml
    [dependencies]
    loess-rs = "*"
    ```

=== "fastLoess (parallel)"

    ```toml
    [dependencies]
    fastLoess = "*"
    ```
