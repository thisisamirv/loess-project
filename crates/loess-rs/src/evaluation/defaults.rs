// Default values for evaluation module (cross-validation, diagnostics, intervals).

// Default number of folds for K-fold cross-validation.
pub const DEFAULT_CV_K_FOLDS: usize = 5;

// Default confidence/prediction interval coverage level.
pub const DEFAULT_INTERVAL_LEVEL: f64 = 0.95;
